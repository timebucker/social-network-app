<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kokolo.socialnetwork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:largeHeap="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.register.RegisterActivity" />
        <activity android:name=".views.login.LoginActivity" />
        <activity android:name=".views.setup.SetupActivity" />
        <activity android:name=".views.post.PostActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".views.findfriend.FindFriendsActivity"
            android:parentActivityName=".views.main.MainActivity" />
        <activity android:name=".views.clickpost.ClickPostActivity" />
        <activity
            android:name=".views.setting.SettingsActivity"
            android:parentActivityName=".views.main.MainActivity" />
        <activity android:name=".views.profile.ProfileActivity" />
        <activity android:name=".views.comment.CommentsActivity" />
        <activity android:name=".views.resetpassword.ResetPasswordActivity" />
        <activity android:name=".views.chat.ChatActivity" />
        <activity android:name=".views.friendslist.FriendsListActivity"></activity>
    </application>

</manifest>